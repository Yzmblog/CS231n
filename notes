Knn（K最近邻算法）：在训练数据中找到k个和想要预测的类别最想的k个类，挑出现次数最多的那个类（即最像的）
    [1]+[2]=[1,2]
但是如果是np.array 则是“对应”元素相加

 首先比较二者的参数部分：

    np.max：(a, axis=None, out=None, keepdims=False) 
        求序列的最值
        最少接收一个参数
        axis：默认为列向（也即 axis=0），axis = 1 时为行方向的最值；
    np.maximum：(X, Y, out=None) 
        X 与 Y 逐位比较取其大者；
        最少接收两个参数
